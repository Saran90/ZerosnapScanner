<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mfcu.zerosnap">

    <application
        android:name=".ZerosnapApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:node="merge">
        <activity android:name="com.mfcu.zerosnap.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.mfcu.zerosnap.InitActivity"
            android:theme="@style/DialogActivity"></activity>
        <activity android:name="com.mfcu.zerosnap.ZerosnapScannerActivity" />
        <activity android:name="com.mfcu.zerosnap.SubscriptionActivity" />

        <meta-data
            android:name="com.razorpay.ApiKey"
            android:value="@string/razor_pay_key" />
    </application>

</manifest>